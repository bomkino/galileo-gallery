# Provenance — Dealer's Pick

## Record

- packet: `R03-dealers-fan`
- provenance class: **historical reference; ownership/rights unknown; principle-only study**
- source pin represented by annex: Galileo Gallery public source `2762043bb733aa28a6c63fe26564504b9f257564`
- mapped historical file: `DealersFan.tsx`
- expected SHA-256: `25a23ccbad51ac3f03c55f9e95f401869cbe6e8c69b5fceac3a47d725ada9452`
- observed SHA-256: `25a23ccbad51ac3f03c55f9e95f401869cbe6e8c69b5fceac3a47d725ada9452`
- observed byte size: `46894`
- observed line count: `1362`
- manifest verification: pass
- reference README: read before mapped file

## Sanitized source identity

The annex contains a historical Framer/React implementation described as a fan driven by parent scroll, viewport progress, or manual progress. It uses a fractional focus helper, a below-stage transform origin, direct DOM transform writes, damped requestAnimationFrame scheduling, optional timed riffle, image ratio/padding controls, captions/counter, and click-focus overlay.

## Observable principles retained

- a perceptibly low pivot can make a hand read physically;
- continuous fractional focus is stronger than integer snapping;
- a bounded neighbour window preserves context and resource limits;
- the crown should straighten/lift and yield through a continuous path;
- mixed ratios need bottom/pivot logic rather than uniform-height coercion;
- focus and keyboard restoration matter when cards overlap.

## Historical behavior explicitly rejected

- parent scroll, viewport, pointer, hover, or damped rAF as serialized/output authority;
- interval/timer-driven flourish;
- direct DOM transform state as Project truth;
- hidden card visibility decisions detached from deterministic story time;
- click overlay as part of Scene identity;
- source lighting, hover lift, or generic focus zoom;
- historical constants, names, CSS, strings, choreography tables, component shape, controls, assets, captions, and external URLs.

## Dependencies and assets observed

Historical source references React, React DOM portals, Framer controls/render-target helpers, DOM observers/listeners, timers, images, and CSS. No dependency, asset, URL, component structure, or style was copied into this packet. The prototype is dependency-free HTML/JS plus a removable SVG evidence renderer and generated fixtures.

## Current live behavior at atelier start

At branch start SHA `c90a7982a981b1a1b2624f5eb0db81e6c2da62b5`, the Product registry exposes `dealers-fan` in the Fan family. The central renderer handles `slide-fan` and `dealers-fan` in one conditional branch with different constants; current motion is therefore not evidence of an independent modern Scene. Current status claims no complete charter/module/integration/evidence/human-verdict chain.

## Zero-code-reuse statement

No source code, comments, CSS, constants, identifiers, strings, assets, URLs, component structure, or choreography table from the historical file was copied or adapted. The evaluator, controls, timeline, fixtures, prototype, and evidence renderer were authored clean-room from the catalogue promise, live Product contracts, observable principles, and this atelier brief.

## Deletion path

Delete `scene-ateliers/atelier-03/dealers-fan/` to remove the entire study. It has no Product import, registry edit, dependency change, workflow change, package change, or shared engine.


## Later public open-source principle study

A separate clean-room pass studied public, pinned Toolcraft, Theatre.js, tldraw, and three.js
materials for interaction ownership, history transactions, sequence transport, renderer selection,
and resource lifecycle. No source, CSS, constants, identifiers, assets, component structure, or
choreography was copied. Exact pins and applied/rejected principles are recorded in
`../TOOLCRAFT_AND_OPEN_SOURCE_STUDY.md`.
