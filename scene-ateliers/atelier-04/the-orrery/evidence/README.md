# The Orrery evidence index

Evidence state: **G10B preflight mechanical comparison in branch; human verdict pending; implementation blocked by G10A**.

## Executable sources

- `../prototype/evaluator.mjs` — pure topologically closed evaluator shared by both renderers.
- `../prototype/app.mjs` and `../prototype/index.html` — canonical DOM/CSS 3D study.
- `../prototype/webgl-comparison/` — raw WebGL2 instanced-atlas comparison.
- `../../gauntlet/verify.mjs` — every exchange boundary, epsilon loop seam, identity, source, lifecycle, and gate checks.
- `../../gauntlet/adversarial.mjs` — ring, scale, direction, ratio, reduced-motion, and parity causality.

## Capture obligations

Fresh external verification must capture identical DOM/WebGL story times, every identity exchange, `duration−ε / duration / 0+ε`, one/two/18/72 items, reverse, reduced motion, transparent composites, context loss/restoration, and source-fidelity probes.

Generated atlas captures prove mechanics only. They do not unblock G10A, activate G10B, integrate Product code, or mint taste approval.
