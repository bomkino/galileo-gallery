# Spiral Image Vortex evidence index

Evidence state: **mechanical DOM/WebGL comparison in branch; human verdict pending**.

## Executable sources

- `../prototype/evaluator.mjs` — one pure semantic evaluator shared by both renderers.
- `../prototype/app.mjs` and `../prototype/index.html` — canonical DOM/CSS 3D study.
- `../prototype/webgl-comparison/` — raw WebGL2 instanced-atlas comparison.
- `../../gauntlet/verify.mjs` — dense 5 ms wrap, continuity, source, lifecycle, and claim checks.
- `../../gauntlet/adversarial.mjs` — control causality, reduced-motion, parity, and metamorphic checks.

## Capture obligations

Fresh external verification must capture canonical and WebGL views at identical story times, real-speed motion, every offstage recycling band, front/back crossings, 2/24/96 items, reverse, reduced motion, transparent composites, context loss/restoration, and source-fidelity probes. Generated atlas captures prove mechanics only.

WebGL promotion remains blocked on human preference, real imported-media RGB/alpha evidence, preview/scrub/export parity, fallback, and bounded GPU memory.
