# Resource and lifecycle notes — Spiral Image Vortex

- One deterministic identity record per source; 127 tested.
- Bounded render view: 23 landscape / 17 portrait.
- No visible source duplicates for seam machinery.
- No WebGL, particle buffer, canvas trail, worker, audio node, or persistent storage.
- Generated fixture images are local data URLs; no network requests.
- One cancellable requestAnimationFrame exists only in prototype transport.
- Same-input/time equality and remount-style reevaluation passed.
- Seam probe shows all wrap samples hidden offstage; endpoint itself is rear depth.
- Crossing probe observed 56 z-order-list changes over 81 deterministic samples.
- Video is represented in evaluator time/resource policy; production decoder/RGB parity remains unproved.
- Capture runner inlines local files because browser navigation is administratively blocked.
