import { CANVASES, DEFAULTS, FIXTURES, evaluateVortex, summarize } from "./evaluator.mjs"
const stage=document.querySelector("#stage"),fixtureSelect=document.querySelector("#fixture"),canvasSelect=document.querySelector("#canvas"),runKindSelect=document.querySelector("#runKind"),scrub=document.querySelector("#scrub"),readback=document.querySelector("#readback"),timeOutput=document.querySelector("#time"),playButton=document.querySelector("#play"),resetButton=document.querySelector("#reset"),transparent=document.querySelector("#transparent"),reducedMotion=document.querySelector("#reducedMotion")
const controlIds=["turns","radialSpread","depthPitch","cardSize","direction"],controls=Object.fromEntries(controlIds.map(id=>[id,document.querySelector(`#${id}`)]))
for(const key of Object.keys(FIXTURES))fixtureSelect.add(new Option(key,key));for(const key of Object.keys(CANVASES))canvasSelect.add(new Option(key,key));fixtureSelect.value="ordinary8";canvasSelect.value="16:9";for(const[key,value]of Object.entries(DEFAULTS))controls[key].value=String(value)
let playing=false,lastStamp=null,frameRequest=null
function svgData(item){const width=Math.max(120,Math.round(240*item.ratio)),height=240,hue=(Number.parseInt(item.id.replace(/\D/g,""),10)*53+208)%360,cut=item.id.includes("mixed")||item.id.includes("media-edge"),background=cut?"none":`hsl(${hue} 42% 86%)`;const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="${height}" viewBox="0 0 ${width} ${height}"><rect width="100%" height="100%" fill="${background}"/><path d="M 0 ${height*.2} C ${width*.25} ${height*.9}, ${width*.7} ${height*.05}, ${width} ${height*.72}" fill="none" stroke="hsl(${hue} 70% 40%)" stroke-width="20"/><circle cx="${width*.32}" cy="${height*.38}" r="${height*.16}" fill="hsl(${(hue+128)%360} 62% 56%)"/><text x="18" y="${height-20}" font-family="monospace" font-size="18" fill="#111722">${item.label}</text></svg>`;return`data:image/svg+xml;charset=utf-8,${encodeURIComponent(svg)}`}
function currentConfig(){return{turns:Number(controls.turns.value),radialSpread:Number(controls.radialSpread.value),depthPitch:Number(controls.depthPitch.value),cardSize:Number(controls.cardSize.value),direction:controls.direction.value}}
function render(){const items=FIXTURES[fixtureSelect.value],canvas=CANVASES[canvasSelect.value],state=evaluateVortex({items,config:currentConfig(),normalizedTime:Number(scrub.value),canvas,runKind:runKindSelect.value,reducedMotion:reducedMotion.checked,spotlightId:items[Math.min(3,items.length-1)]?.id,finaleId:items.at(-1)?.id});stage.style.width=`${canvas.width}px`;stage.style.height=`${canvas.height}px`;stage.classList.toggle("is-transparent",transparent.checked);stage.replaceChildren();if(!transparent.checked){const guide=document.createElement("svg");guide.className="thread-guide";guide.setAttribute("viewBox",`0 0 ${canvas.width} ${canvas.height}`);guide.innerHTML=`<path d="M ${canvas.width*.5} -40 C ${canvas.width*.05} ${canvas.height*.2}, ${canvas.width*.95} ${canvas.height*.35}, ${canvas.width*.5} ${canvas.height*.5} C ${canvas.width*.05} ${canvas.height*.65}, ${canvas.width*.95} ${canvas.height*.8}, ${canvas.width*.5} ${canvas.height+40}" fill="none" stroke="#2458b8" stroke-width="2" stroke-dasharray="4 10"/>`;stage.append(guide)}for(const card of state.renderSlots){const node=document.createElement("div");node.className="card";node.dataset.mediaId=card.id;node.style.width=`${card.width}px`;node.style.height=`${card.height}px`;node.style.left=`${card.x-card.width/2}px`;node.style.top=`${card.y-card.height/2}px`;node.style.zIndex=String(card.zOrder);node.style.transform=`scale(${card.scale}) perspective(1200px) rotateY(${card.rotateYDeg}deg) rotateZ(${card.rotateZDeg}deg)`;const shell=document.createElement("div");shell.className="art-shell";const item=items[card.sourceIndex];if(item.kind==="failed"){const failed=document.createElement("div");failed.className="failed";failed.textContent=`${item.label} / FAILED MEDIA`;shell.append(failed)}else{const image=document.createElement("img");image.alt=item.label;image.draggable=false;image.src=svgData(item);shell.append(image)}node.append(shell);stage.append(node)}timeOutput.value=Number(scrub.value).toFixed(3);readback.textContent=JSON.stringify({...summarize(state),fixture:fixtureSelect.value,canvasPreset:canvasSelect.value,transparent:transparent.checked,reducedMotion:reducedMotion.checked,exactState:state.cards.map(card=>({id:card.id,u:+card.u.toFixed(6),x:+card.x.toFixed(3),y:+card.y.toFixed(3),depth:+card.depth.toFixed(6),visible:card.visible,seamZone:card.seamZone,nearPass:card.nearPass}))},null,2);document.documentElement.dataset.ready="true";window.__lastState=state;return state}
function tick(stamp){if(!playing)return;if(lastStamp===null)lastStamp=stamp;const delta=Math.min(100,stamp-lastStamp);lastStamp=stamp;scrub.value=String((Number(scrub.value)+delta/9000)%1);render();frameRequest=requestAnimationFrame(tick)}function setPlaying(value){playing=value;playButton.textContent=playing?"Pause":"Play";lastStamp=null;if(frameRequest)cancelAnimationFrame(frameRequest);if(playing)frameRequest=requestAnimationFrame(tick)}function reset(){setPlaying(false);fixtureSelect.value="ordinary8";canvasSelect.value="16:9";runKindSelect.value="loop";scrub.value="0";transparent.checked=false;reducedMotion.checked=false;for(const[key,value]of Object.entries(DEFAULTS))controls[key].value=String(value);render()}
for(const element of[fixtureSelect,canvasSelect,runKindSelect,scrub,transparent,reducedMotion,...Object.values(controls)])element.addEventListener("input",render);playButton.addEventListener("click",()=>setPlaying(!playing));resetButton.addEventListener("click",reset);window.prototypeApi=Object.freeze({render,setTime(value){scrub.value=String(value);return render()},setFixture(value){fixtureSelect.value=value;return render()},setCanvas(value){canvasSelect.value=value;return render()},setTransparent(value){transparent.checked=Boolean(value);return render()},setControl(id,value){controls[id].value=String(value);return render()},reset});render()
