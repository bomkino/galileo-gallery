# Deck River isolated prototype

Dependency-free fixed-camera corridor study. Generated fixtures only. Never imported by Product.

```text
open index.html
node check.cjs
```

Capture API: `window.atelierPrototype` exposes story time, source count, canvas, direction, reduced
motion, composite, silhouette, and `getState()`.
