# Isolated prototype

Dependency-free generated-fixture study for the candidate `deck-river-loader` / Chapter Reveal charter. It is not imported by Galileo Gallery and contains no Product implementation.

The evaluator is finite and clamped. It does not loop, read visit state, inspect query strings, create portals, mutate `body`, or own audio. Its visual skip maps only to a compiled Timeline landmark.

Run: `node check.cjs`. The evidence runner inlines the same prototype bytes because managed Chromium blocks `file://` and local listening sockets.
