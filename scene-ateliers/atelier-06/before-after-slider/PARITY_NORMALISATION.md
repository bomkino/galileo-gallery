# Parity normalisation — `before-after-slider`

## Added

Versioned Scene, acceptance, render, workload, interaction, source-locator and receipt contracts. Original authored files and prototype remain. Original control contract is preserved as `ORIGINAL_CAPABILITY_AND_CONTROLS.json`.

## Authority corrections

- `sweep-duration` → Timeline (`timeline.segment.sweep-duration`)
- `turnaround-hold` → Timeline (`timeline.hold.turnaround`)

## Identity

Two exactly aligned sources share one content rectangle while one reversible divider sweeps, rests with zero velocity, turns gently, and returns without a hard reset.

Human verdict remains pending.
