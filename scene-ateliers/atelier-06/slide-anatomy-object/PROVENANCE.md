# Provenance — Slide Anatomy

## Classification

Clean-room, principle-level flat-source candidate.

## Inputs inspected

- live Gallery product decisions and implementation status;
- live catalogue promise for a frame that assembles, holds, and returns;
- common Scene Atelier contract;
- current Project, Timeline, source-fidelity, Look, audio, export, accessibility, and lifecycle boundaries;
- observable cutaway/exploded-view principles, limited to Project-known apparatus.

## Historical source use

No historical `SlideAnatomyObject` implementation was opened or adapted. No `R03-slide-anatomy-object` packet was instantiated. No historical code, comments, constants, identifiers, assets, CSS, component structure, or timing values were copied.

## Semantic-layer restraint

No segmentation model, OCR, computer vision, design-file parser, or source-pixel inference was used. The candidate deliberately treats imported artwork as one plane. Apparatus names originate only from explicit Project concepts.

## External resources

None. The prototype loads no network code, fonts, media, analytics, or telemetry. Fixtures are locally generated and declarative.

## Rights boundary

This packet does not claim rights over user media, design files, historical assets, or future authored-layer inputs. Any later authored-layer contract requires its own provenance and licence record.
