# Edge, resource, and accessibility contract — The Build

## Input states

- **Zero sources:** empty guidance; no fabricated final slide.
- **One source:** valid flat-source preflight.
- **More than one source:** preserve identities; require explicit source selection.
- **No matte/guides/caption:** compile only world, source, and finale; contract duration.
- **Empty caption:** no caption stage, node, or timing allowance.
- **Failed source:** stable source-stage placeholder and explicit finale consequence.
- **Video source:** paused native playback, deterministic story-time seek.
- **Transparent source:** alpha preserved; apparatus separate.

## Duration safety

The compiler reports the current readable floor next to requested duration. Fixed durations below the floor are rejected with causal copy: identify the minimum and which present stages create it. It never silently drops a stage, shortens a leg below readability, or speeds the finale into a flash.

## Geometry safety

Stage lanes are deterministic, non-crossing, and inside the safe area at every canonical sample. Established stages remain stable while the next arrives. The source remains readable throughout and owns the highest final priority. Portrait canvases move stage ledger and labels outside the hero plane.

## Resource bound

Flat-source preflight: at most five stage records, five apparatus nodes, one source media node, one stage ledger, one animation handle, one resize observer, and one keyboard listener. Stable keyed nodes receive state updates; no per-frame replacement. Disposal cancels animation, disconnects observers, pauses media, clears seeks, removes listeners, and revokes generated URLs.

Future authored-stage mode must declare independent bounds before implementation. It may not inherit an unbounded stage table.

## Keyboard

- Space: play/pause.
- Left/Right: previous/next stage.
- Home: phrase start.
- End: finale.
- Enter: hold current stage for review.
- Up/Down: move through the textual stage ledger.

Controls have visible focus and at least 44 × 44 px targets. Current stage, stage count, duration, readable floor, finale state, and `preflight—not authored process` are exposed as text.

## Announcements

Announce explicit play/pause, mode changes, stage selection, duration rejection, and finale arrival once. Never announce per-frame progress.

## Reduced motion

Render the intact final source and static stage ledger. No automatic arrivals, cursor simulation, pulsing, parallax, or finale growth. Manual ledger selection changes textual emphasis only.

## Fallback

If transforms are unavailable, render the final source with a sequential textual stage ledger and optional static apparatus thumbnails. Never substitute a generic assembly animation or fabricated process graphic.
