name: Legacy archive checks
on:
  push:
    branches: ['codex/legacy/**']
permissions:
  contents: read
jobs:
  archive:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: npm ci && npm test
